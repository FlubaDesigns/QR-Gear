# QR Gear – Replit Control File (ENFORCED)

---

## 🔁 CONTINUOUS REFRESH RULE (MANDATORY)

Before EVERY response that includes:
- code
- file changes
- debugging
- system decisions

You MUST re-confirm alignment with this file.

---

### REQUIRED REFRESH CHECK

You MUST internally verify:

- Execution rules still apply
- Naming standards still apply
- Authority files still control logic
- Code must be traced before changes
- No guessing is allowed

---

### IF UNSURE

If you are not certain you are aligned with this file:

- Re-read REPLIT.md
- Re-read relevant authority files
- THEN proceed

---

### FAILURE CONDITION

If this refresh step is skipped:

→ Response is invalid  
→ Must restart with:

RESTART REQUIRED: Refresh step skipped.

---

## 📑 TABLE OF CONTENTS (AUTO-MAINTAINED)

1. Purpose
2. Execution Gate
3. Relationship to README
4. Required Read Order
5. Required Confirmation
6. Continuous Refresh Rule (MANDATORY)
7. Authority File Pointers
8. Naming Standards Requirement
9. Code-First Rule
10. QRG Law
11. Architecture Snapshot
12. Deployment Process
13. Post-Deploy Verification
14. Final Response Contract
15. Failure Rules
16. TOC Maintenance Rule
17. Canonical Core Directive

⚠️ RULE:
- This Table of Contents MUST be updated whenever sections are added, removed, or renamed.
- If TOC is outdated → task is considered incomplete.

---

## 1. PURPOSE

This file is the execution control layer for QR Gear.

README.md is the entry point.  
This file enforces behavior AFTER entry.

This file is NOT a documentation dump.  
This file is a CONTROL SYSTEM.

---

## 2. EXECUTION GATE

STOP.

You are NOT allowed to:

- modify files
- suggest implementation
- write code
- run commands
- deploy
- rename structures
- assume behavior

UNTIL required reads and confirmation are complete.

Failure = invalid response.

---

## 3. RELATIONSHIP TO README

README.md is REQUIRED FIRST.

If README.md was not read:

STOP.

Respond EXACTLY:

RESTART REQUIRED: README flow not completed.

This file does NOT replace README.  
This file is entered FROM README.

---

## 4. REQUIRED READ ORDER

You MUST complete this sequence:

1. Read ALL skills  
2. Read README.md (FULL)  
3. Read THIS FILE (FULL)  
4. Read authority files:
   - QRG.md
   - BLD.md
   - GRF.md
   - ASSEMBLY.md  
5. Read:
   - NAMING_STANDARDS.md  
6. Read affected code BEFORE changes  

No skipping.  
No summarizing.  
No relying on memory.

---

## 5. REQUIRED CONFIRMATION

Before ANY implementation, output EXACTLY:

CONFIRMED:
- Skills loaded
- README.md read in full
- REPLIT.md read in full
- Authority files read in full
- NAMING_STANDARDS.md read in full
- Affected code traced before changes

If this cannot be truthfully produced:

→ STOP  
→ DO NOT PROCEED  

---

## 6. CONTINUOUS REFRESH RULE (MANDATORY)

This prevents drift across long sessions.

---

### 🔁 REFRESH REQUIREMENT

Before EVERY response that includes:

- code
- file edits
- debugging
- system decisions
- architecture decisions

You MUST perform a refresh check.

---

### ✅ REFRESH CHECK (MANDATORY)

You MUST internally confirm:

- Execution rules are still active
- Naming standards are still enforced
- Authority files still control logic
- Code must be traced before changes
- No guessing is allowed

---

### 🔄 CONDITIONAL RE-READ

If ANY uncertainty exists:

You MUST:

1. Re-read REPLIT.md  
2. Re-read relevant authority file(s)  
3. THEN proceed  

---

### 🚫 FAILURE CONDITION

If refresh step is skipped:

Respond EXACTLY:

RESTART REQUIRED: Refresh step skipped.

DO NOT continue.

---

## 7. AUTHORITY FILE POINTERS

System truth lives in:

- QRG.md → QR identity system  
- BLD.md → build logic  
- GRF.md → graphics system  
- ASSEMBLY.md → final composition  

Rules:

- Do NOT duplicate logic here  
- Do NOT override authority files  
- Always consult them before changes  

---

## 8. NAMING STANDARDS REQUIREMENT

All naming rules are in:

NAMING_STANDARDS.md

MANDATORY:

- Do NOT invent names  
- Do NOT rename existing structures  
- Match codebase patterns EXACTLY  

Applies to:

- files
- classes
- IDs
- components
- Firestore collections
- Firestore fields
- routes
- APIs
- packet structures
- graphic layers

---

## 9. CODE-FIRST RULE

Documentation is NOT enough.

Before ANY change:

You MUST trace:

- where data starts
- how data transforms
- where data is saved
- where data is loaded
- where data is rendered
- what naming is used
- what depends on it

No code read = no authorization.

---

## 10. QRG LAW

All QRG identity rules are defined in:

- `QRG.md`

Read QRG.md directly. Do NOT rely on secondary descriptions.

QRG.md is authoritative. This section is a pointer only.

---

## 11. ARCHITECTURE SNAPSHOT

Stack:

- Frontend → React / Vite  
- Backend → Firebase Functions  
- Database → Firestore  
- Hosting → Firebase  

Entry point:

functions/src/index.ts

RULE:

- index.ts = wiring ONLY  
- no business logic  

---

## 12. DEPLOYMENT PROCESS

Production-only mode.

Run EXACTLY:

1. bash deploy/1-build.sh  
2. bash deploy/2-functions.sh  
3. bash deploy/3-hosting.sh  

FORBIDDEN:

- chaining scripts  
- skipping steps  
- alternate deploy commands  
- claiming success without verification  

---

## 13. POST-DEPLOY VERIFICATION

You MUST verify:

- build success  
- functions deployed  
- hosting deployed  
- feature works  
- no runtime errors  

If verification fails:

→ FAIL LOUDLY  
→ DO NOT claim completion  

---

## 14. FINAL RESPONSE CONTRACT

Every final response MUST include:

1. What was changed  
2. Exact files modified  
3. Why changes were made  
4. How code was traced  
5. How it was verified  
6. Deployment status  
7. Confirmation refresh check was performed  

Missing ANY item = incomplete.

---

## 15. FAILURE RULES

If README not read:

RESTART REQUIRED: README flow not completed.

If confirmation missing:

RESTART REQUIRED: Confirmation missing.

If refresh skipped:

RESTART REQUIRED: Refresh step skipped.

If code not traced:

RESTART REQUIRED: Code was not traced.

If deploy required but skipped:

DEPLOYMENT INCOMPLETE.

If verification fails:

VERIFICATION FAILED.

---

## 16. TOC MAINTENANCE RULE

Whenever this file changes:

- Update Table of Contents  
- Keep section numbers accurate  
- Keep section names accurate  
- Maintain authority references  

If TOC is outdated → task incomplete.

---

## 17. CANONICAL CORE DIRECTIVE

The following files ARE the system:

- `BLD.md` — Build Structure
- `GRF.md` — File Identity
- `QRG.md` — Instance Identity
- `ASSEMBLY.md` — Mapping Layer

These four documents form the COMPLETE and ONLY definition of system behavior.

Rules:

1. No other file may redefine their logic, summarize their rules in a way that changes meaning, introduce alternative structures, or extend their responsibilities.

2. All other documents are supporting, descriptive, and non-authoritative.

3. If any conflict exists:
   → BLD / GRF / QRG / ASSEMBLY win
   → All other sources are invalid

4. These files must be read directly — never rely on secondary descriptions.

5. Agents must not infer behavior outside these files, fill gaps with assumptions, or create fallback interpretations.

If an agent attempts to use a non-canonical definition, deviate from these files, or introduce alternate logic:

→ STOP
→ FLAG VIOLATION
→ RETURN TO CANONICAL CORE
